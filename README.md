#Questions

1️⃣ What is the difference between var, let, and const?
2️⃣ What is the spread operator (...)?
3️⃣ What is the difference between map(), filter(), and forEach()?
4️⃣ What is an arrow function?
5️⃣ What are template literals?

#Answers:

1️⃣ ## 'Var' 'Let' 'Const'
Var, Let and Const are basically a Feature Scopes that could use to declare something.

The main difference is VAR= is a function scoped that can be declared outside from a function or anywhere.which is why it creates bugs in big projects

Let= can be used to change only inside '{}' but can't declare.

const = it can be use once after used it can't be changed



2️⃣ ## Sprade_Oparetor :
 Sprade Operator is used to link multiple array in one array or it mainly sprades it with new one. which is better to use it in objects and copy so that original got safe.



3️⃣ ## 'map()' 'filter()' 'forEach()' 
map()- Transfroms everything. it can be used if we neeed to change every elements.

filter()- only needed elements. it can be used if we need to delete some elements

forEach()- it basically used on small data for print without returning something.



4️⃣ ## Arrow Function :
Arrow Function is a thing from ES6 to minimize a normal function into a short from.

if i set a example-
Old version: function add(a, b) {
  return a + b;
}

After arrow Function: const add = (a, b) => a + b;


5️⃣ ## Template Literals

Template Literals is basically string literals in JS that can be used in any thing like add use string interpolaton  by  using backtick ( ` ), symbol ( ${..} )
these are the use methods to execute a long code into a shorter from. for using this code looks clean and easy to read. 

